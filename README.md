# AI 智能录入 · 对话式 — POC Demo Agent

> 由交互原型 `AI_智能录入_独立交互原型.html` 100% 转化而来的**可运行、模块化、可测试**工程。
> 适合个人测试、内测、客户体验。

一个面向销售线索录入场景的「AI 智能录入」演示：用户可以**一句话说完**、**让 AI 一项项问**、**上传文档/图片 OCR**、**语音输入**、**批量导入**，AI 实时把自然语言解析成结构化线索字段并回填到右侧表单，支持 CRM 自动匹配、AI 创建客户/联系人、编码自动转换、线索分级建议等。

---

## ✨ 已完整保留的交互（每一处可点击均有反应）

| 区域 | 交互 | 说明 |
| --- | --- | --- |
| 启动 | 打开即进入「AI 智能录入」面板 | 左对话区 + 右字段卡两栏布局 |
| 对话区 | **发送**（按钮 / Enter） | 自由文本 → `smartExtract` 抽取 → 实时回填右侧字段 |
| 对话区 | **语音输入** 🎤 | 点击开始/结束「录音」，模拟转写填入输入框 |
| 对话区 | **+ 菜单** | 上传文件（文档/图片）、OCR 扫描、批量导入 三入口 |
| 对话区 | **4 个示例气泡** | 微信需求 / 电话沟通 / 招标线索 / 转介绍，点选即填满字段 |
| 字段卡 | **内联编辑** | 每个字段可直接在输入框/下拉里改，实时更新完成度 |
| 字段卡 | **完成度进度条** | 按已填字段数动态计算百分比 |
| 字段卡 | **缺失红点/红框** | 关键字段缺失时高亮标注 |
| 字段卡 | **重置 / 保存线索** | 保存写入「已保存列表」，弹 Toast，清空表单续录 |
| 字段编辑弹窗 | 打开 / 修改 / 保存 | 级别字段附带「判定标准」说明 |
| 分级标准弹层 | `showLevelCriteria` | S/A/B/C 四级完整说明，可开可关 |
| 编码对照弹层 | `showCodeMap` | 行业编码 / 客户等级编码 → 显示值映射 |
| CRM 匹配 | 自动比对业主/联系人 | 命中显示「CRM 已存在」，未命中提供「AI 创建」入口 |
| AI 创建实体 | 粘贴一段话解析生成 | 自动生成客户/联系人并关联到本线索 |
| 批量导入 | 进度 → 预览 → 保存 | 12 条样本，含「部分缺失/需确认」行可点击补全 |
| 行编辑弹窗 | 补全缺失字段 | 补全后状态标签自动更新（已识别/部分缺失/需确认） |
| 已保存线索 | 弹层列表预览 | 含完整度百分比 |
| 关闭面板 | 回到启动卡 | 可重新打开 / 查看已保存线索 |

> 所有示例数据（鸿图智造、东方汽车、南方新能源、安泰科技…）与原型完全一致，原样保留。

---

## 🚀 快速开始

### 给客户演示（最省事）

**双击根目录的 `启动演示.cmd`** —— 它会自动装好依赖（仅首次）、起服务器、打开浏览器。
演示期间保持那个黑窗口开着即可；关窗口或按 `Ctrl+C` 停止。

### 或用命令行

```bash
# 1. 安装依赖（已安装可跳过）
npm install

# 2. 启动本地 Demo（默认 http://localhost:5173，自动打开浏览器）
npm run dev
```

> 环境要求：Node.js ≥ 18。**图标已本地化打包，全程无需联网**（除非重新 `npm install`）。

### 各命令什么时候用（演示只用第 1 条）

| 命令 | 何时用 | 演示必需？ |
| --- | --- | --- |
| `npm run dev` | **演示 / 日常体验**，启动本地服务器 | ✅ 唯一必需 |
| `npm test` | 改了代码想复验逻辑（103 个用例） | ❌ 可选 |
| `npm run build` | 要部署到服务器 / 拷成静态文件离线分发 | ❌ 可选 |
| `npm run preview` | 预览 `build` 出来的产物 | ❌ 可选 |
| `npm run test:watch` | 边改代码边自动跑测试 | ❌ 可选 |
| `npm run coverage` | 看测试覆盖率（聚焦 `core/` 与 `data/`） | ❌ 可选 |

---

## 🗂️ 项目结构

```
ai-smart-entry-demo/
├─ index.html                 # 入口页（#app 容器 + 两个静态弹窗骨架）
├─ vite.config.js             # Vite + Vitest 统一配置（jsdom 测试环境）
├─ package.json
├─ src/
│  ├─ main.js                 # 应用入口：绑定容器 / 暴露 window 全局 / 启动面板
│  ├─ state.js                # 运行时共享状态（C / C_MAIN / SAVED_LEADS …）
│  ├─ styles/
│  │  └─ main.css             # 全部样式（从原型 1:1 提取）
│  ├─ core/                   # ★ 纯函数引擎（无 DOM 依赖，100% 可单测）
│  │  ├─ smartExtract.js      #   自然语言 → 结构化字段 抽取引擎
│  │  ├─ suggestLevel.js      #   按预算+关键词建议线索级别
│  │  ├─ normalizeTitle.js    #   职位归一化（采购总监 / 项目负责人 …）
│  │  ├─ completeness.js      #   线索完整度百分比
│  │  └─ dom.js               #   DOM 助手 d() / esc()
│  ├─ data/                   # 数据与配置（与原型一致）
│  │  ├─ fields.js            #   字段定义 F/FL/FI/FGROUPS/SELS/KEY_FIELDS …
│  │  ├─ codeMaps.js          #   行业 / 客户等级 编码表
│  │  ├─ crm.js               #   CRM 已有客户库（运行时可追加）
│  │  ├─ criteria.js          #   线索分级标准
│  │  └─ examples.js          #   对话示例 4 条 + 批量导入样本 12 条
│  └─ ui/                     # UI 模块（DOM 渲染 + 交互）
│     ├─ fields.js            #   字段预览卡：渲染/进度/缺失标注/编码徽标
│     ├─ modals.js            #   字段编辑 / 分级标准 / 编码对照 弹窗
│     ├─ crmMatch.js          #   CRM 匹配 + AI 创建客户/联系人
│     ├─ chat.js              #   对话区：气泡/语音/发送/文档·图片 OCR
│     ├─ leads.js             #   保存/Toast/提示条/已保存列表
│     ├─ importLeads.js       #   批量导入：进度/预览/行补全/提交
│     └─ panel.js             #   AI 智能录入主面板（两栏组装）+ 启动卡
└─ test/                      # 单元 + 集成测试（103 个用例）
   ├─ smartExtract.test.js    #   抽取引擎（39）
   ├─ suggestLevel.test.js    #   级别建议（9）
   ├─ normalizeTitle.test.js  #   职位归一化（6）
   ├─ completeness.test.js    #   完整度（5）
   ├─ dom.test.js             #   DOM 助手（8）
   ├─ data.test.js            #   数据一致性（13）
   ├─ fields.dom.test.js      #   字段卡 DOM 渲染（6）
   └─ integration.dom.test.js #   端到端交互冒烟（17）
```

---

## 🏗️ 架构说明

原型是一份单文件 HTML（约 1700 行，全局函数 + 内联 onclick）。本工程在**逐行保真移植**的前提下做了分层：

1. **`core/` 纯函数层** — 抽取引擎与判定逻辑完全剥离 DOM，输入字符串、输出对象，便于穷举单测。这是 Demo 的「大脑」。
2. **`data/` 数据层** — 字段定义、编码表、CRM 库、示例样本，集中且可复用；运行时共享同一引用（CRM 库 / 导入数据会被动态增删）。
3. **`ui/` 视图层** — 每个功能区一个模块，内部用 `import` 互相调用；ES Module 的**实时绑定**天然支持模块间循环引用（所有调用都发生在运行时事件回调里，模块加载期无副作用）。
4. **`state.js` 状态层** — 把原型里会被「重新赋值」的全局量（如字段卡容器 `C`）集中到一个对象，保证跨模块看到同一份引用。
5. **`main.js` 装配层** — 绑定容器、把动态 HTML 里 `onclick="fn()"` 依赖的函数暴露到 `window`、接管静态模板的 `[data-action]` 按钮、启动面板。

> 抽取引擎、字段定义、示例数据均与原型**逐字对齐**，行为一致。

---

## 🧪 测试与调试

- **测试框架**：Vitest + jsdom（浏览器环境模拟）。
- **覆盖范围**：
  - 纯函数引擎：预算单位换算、编码自动转换、联系人/职位识别、严格不臆测原则…
  - 数据一致性：字段/标签/图标一一对应，示例样本结构完整。
  - **端到端集成**：在 jsdom 中真实启动应用，逐一触发「打开面板 / 点示例 / 发送 / 保存 / 编辑弹窗 / 分级标准 / 编码对照 / CRM 匹配 / AI 创建实体 / 批量导入 / 关闭面板」等交互并断言其反应——等价于自动化点击验证。

```bash
npm test            # 全部 103 个用例
npm run test:watch  # 改代码自动重跑
```

---

## 🛠️ 技术栈

- **构建/开发**：Vite 6
- **测试**：Vitest 2 + jsdom 25
- **运行时**：原生 ES Modules + 原生 DOM（零运行时框架依赖）
- **图标**：Tabler Icons（**本地依赖打包，离线可用**，随 `npm install` 一并安装）

---

## 📌 说明

- 本项目为 **POC 演示**：所有「AI 识别」均为前端规则引擎（正则 + 关键词 + 编码表）模拟，**不调用任何后端或大模型**，开箱即跑、**完全离线可用**（含图标）。这正是为「随时给客户演示、确定性、零成本、不依赖网络」而做的取舍。
- 如需接入**真实大模型**：在 `src/core/smartExtract.js` 之后挂一层 API 适配（把文本发给模型、拿回字段对象），UI 与字段定义无需改动 —— 架构已为此预留。届时建议同时接后端做线索持久化。
- 「上传文件 / OCR / 语音」为交互演示：用预置样本模拟识别结果，体现产品流程而非真实文件解析。
- 涉及保存/导入的数据仅存于当前浏览器会话内存，刷新即清空。
