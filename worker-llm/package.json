{
  "name": "smart-entry-llm",
  "private": true,
  "version": "1.0.0",
  "description": "手搓 Agent 的 LLM 代理（Cloudflare Worker）—— 为 AI 智能录入 Demo 保管大模型密钥、解决 CORS，并把 OpenAI 形状的对话转发给所选厂商（含 Claude 翻译）",
  "scripts": {
    "login": "wrangler login",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
